먼저 visual studio를 다운로드 해준다 
그 후 visual studio 검색창에서 c++을 다운받아준다
그 후 c++컴파일을 위해 build-essential 패키지를 다운 받아준다
터미널에 입력
$ sudo apt update
$ sudo apt install build-essential
설치 후 gcc --version 명령어를 실행하여 build-essential 패키지가 정상적으로 설치가 완료되었는지 확인할 수 있다.
